<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="hr_leave_type_view_form_inherit_custom_attendance" model="ir.ui.view">
        <field name="name">hr.leave.type.view.form.inherit.custom.attendance</field>
        <field name="model">hr.leave.type</field>
        <field name="inherit_id" ref="hr_holidays.edit_holiday_status_form"/>
        <field name="arch" type="xml">

            <xpath expr="//form" position="attributes">
                <attribute name="create">0</attribute>
                <attribute name="delete">0</attribute>
            </xpath>

            <xpath expr="//field[@name='support_document']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <xpath expr="//group[@name='negative_cap']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <xpath expr="//group[@name='leave_validation']" position="inside">
                <field name="employee_id" string="Người lập"/>
            </xpath>

            <xpath expr="//field[@name='responsible_ids']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

        </field>
    </record>

    <record id="hr_leave_type_view_tree_inherit_custom_attendance" model="ir.ui.view">
        <field name="name">hr.leave.type.view.tree.inherit.custom.attendance</field>
        <field name="model">hr.leave.type</field>
        <field name="inherit_id" ref="hr_holidays.view_holiday_status_normal_tree"/>
        <field name="arch" type="xml">

            <xpath expr="//tree" position="attributes">
                <attribute name="create">0</attribute>
                <attribute name="delete">0</attribute>
            </xpath>

            <xpath expr="//field[@name='display_name']" position="after">
                <field name="employee_id" string="Người lập"/>
            </xpath>
        </field>
    </record>

    <record id="action_tedi_work_entry_grid" model="ir.actions.client">
        <field name="name">Bảng chấm công tổng hợp</field>
        <field name="tag">tedi_work_entry_monthly_grid</field>
    </record>

</odoo>